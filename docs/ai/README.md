# docs/ai (cursor-base)

This repository is the **canonical shared Cursor toolkit** (`cursor-base`). It is intentionally **not** a full application codebase, so this `docs/ai/` directory is a **minimal stub** to satisfy agent entry-point expectations referenced by `.cursor` rules.

## Where to look instead

- **Toolkit mechanics + split `.cursor` adoption:** [`docs/dev/cursor-kit.md`](../dev/cursor-kit.md)
- **Universal kit documentation (often symlinked):** `.cursor/docs/` (start at `.cursor/docs/README.md`)
- **Separation contract:** `.cursor/context/project-docs-contract.md`

## Consumer repositories

In a normal product repository, replace this stub with a real `docs/ai/README.md` and the rest of your adoption set (`AGENT_ADOPTION.md`, `source-of-truth.md`, navigation docs, and so on).
