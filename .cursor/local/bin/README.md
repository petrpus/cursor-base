# Local Cursor Helpers

This directory is for local-only helper scripts used by Cursor hooks and runtime workflows.
Do not rely on these scripts from CI unless you explicitly choose to version them elsewhere.
