# Main Orchestration (reference)

The **enforceable** policy is in:

- `.cursor/rules/orchestration/main-orchestration.mdc` (tier L1–L4, plan-review, session budget, closeout)
- `.cursor/rules/orchestration/risk-tiering.mdc` (L2 plan floor, L3 economy)

Delegation text was merged from `delegation-dna.mdc` into the `.mdc` file. Specialist matrix: [orchestration-routing.md](../../docs/orchestration-routing.md) under `.cursor/docs/`.

This markdown file is a human pointer only; edit the `.mdc` for behavior changes.
